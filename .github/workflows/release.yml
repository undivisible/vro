name: Release

on:
  push:
    tags:
      - "v*"

permissions:
  contents: write

jobs:
  build:
    strategy:
      fail-fast: false
      matrix:
        include:
          - os: macos-latest
            asset_suffix: macos-aarch64
            ext: tar.gz
            bin: vro
          - os: ubuntu-latest
            asset_suffix: linux-x86_64
            ext: tar.gz
            bin: vro
          - os: ubuntu-24.04-arm
            asset_suffix: linux-aarch64
            ext: tar.gz
            bin: vro
          - os: windows-latest
            asset_suffix: windows-x86_64
            ext: zip
            bin: vro.exe
    runs-on: ${{ matrix.os }}
    steps:
      - uses: actions/checkout@v4

      - name: Build V from source (unix)
        if: matrix.os != 'windows-latest'
        shell: bash
        run: |
          set -euo pipefail
          git clone --depth 1 https://github.com/vlang/v.git "${HOME}/vlang"
          cd "${HOME}/vlang"
          make
          echo "V=${HOME}/vlang/v" >> "${GITHUB_ENV}"

      - name: Build V from source (windows)
        if: matrix.os == 'windows-latest'
        shell: bash
        run: |
          set -euo pipefail
          mkdir -p "${HOME}/vlang"
          curl -fsSL "https://github.com/vlang/v/releases/download/0.5.1/v_windows.zip" -o /tmp/v.zip
          unzip -q /tmp/v.zip -d "${HOME}/vlang"
          V_EXE=$(find "${HOME}/vlang" -name v.exe -type f | head -1)
          test -n "${V_EXE}"
          echo "V=${V_EXE}" >> "${GITHUB_ENV}"

      - name: Verify vfmt (unix)
        if: matrix.os != 'windows-latest'
        shell: bash
        run: |
          set -euo pipefail
          cd "${GITHUB_WORKSPACE}"
          "${V}" fmt -verify main.v input.v syntax.v syntax_test.v

      - name: Format sources (windows)
        if: matrix.os == 'windows-latest'
        shell: bash
        run: |
          set -euo pipefail
          cd "${GITHUB_WORKSPACE}"
          "${V}" fmt -w main.v input.v syntax.v syntax_test.v

      - name: Build vro
        shell: bash
        run: |
          set -euo pipefail
          "${V}" -gc none -check .
          "${V}" -gc none test .
          "${V}" -gc none -prod -o ${{ matrix.bin }} .

      - name: Verify binary
        shell: bash
        run: |
          set -euo pipefail
          test -x ./${{ matrix.bin }} || test -f ./${{ matrix.bin }}
          ./${{ matrix.bin }} -version
          ./${{ matrix.bin }} --help >/dev/null

      - name: Package (tar.gz)
        if: matrix.ext == 'tar.gz'
        shell: bash
        run: |
          set -euo pipefail
          mkdir -p dist
          cp ${{ matrix.bin }} "dist/${{ matrix.bin }}"
          cp -R syntax "dist/syntax"
          (cd dist && tar -czf "vro-${{ matrix.asset_suffix }}.tar.gz" ${{ matrix.bin }} syntax)
          (cd dist && shasum -a 256 "vro-${{ matrix.asset_suffix }}.tar.gz" > "vro-${{ matrix.asset_suffix }}.sha256")

      - name: Package (zip)
        if: matrix.ext == 'zip'
        shell: pwsh
        run: |
          $ErrorActionPreference = 'Stop'
          New-Item -ItemType Directory -Force dist | Out-Null
          Copy-Item ${{ matrix.bin }} dist/
          Copy-Item -Recurse syntax dist/syntax
          Set-Location dist
          Compress-Archive -Path ${{ matrix.bin }},syntax -DestinationPath "vro-${{ matrix.asset_suffix }}.zip"
          (Get-FileHash "vro-${{ matrix.asset_suffix }}.zip" -Algorithm SHA256).Hash.ToLower() + "  vro-${{ matrix.asset_suffix }}.zip" | Out-File -FilePath "vro-${{ matrix.asset_suffix }}.sha256" -Encoding ascii

      - name: Upload artifacts
        uses: actions/upload-artifact@v4
        with:
          name: release-${{ matrix.asset_suffix }}
          path: dist/*

  publish:
    runs-on: ubuntu-latest
    needs: build
    steps:
      - name: Download artifacts
        uses: actions/download-artifact@v4
        with:
          path: dist
          merge-multiple: true

      - name: Publish GitHub release
        uses: softprops/action-gh-release@v2
        with:
          files: dist/*
